@model List<WishList.Models.Item>
<h1>Wishlist</h1>

<ul>
    @foreach (var item in @Model)
   {
       <li>@item.Description <a asp-action="delete" asp-all-route-id="@item.Id">delete</a></li>
   }
</ul>